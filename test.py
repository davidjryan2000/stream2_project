@app.route("/")
def donor_projects():

    FIELDS = {
        '_id': False, 'MarketCap': True, 'TotalinCirculation': True, 'TradeVolumeUSDExchange': True,
        'TotalTransactionFeesinUSD': True, 'CostperTransactionUSD': True,
        'TotalDailyTransactions': True, 'TotalTransactions': True,
        'GOLD/USD':True, 'Brent Oil/USD': True,



var chartWidth = $("#pieChart").width();
var pieRadius = 200;
if(charWidth >= 480){
    pieradius = 200;    
} else {
    pieRadius = chartWidth * 0.3;
}
...abs

    .width(chartWidth)
    .radius(pieRadius)